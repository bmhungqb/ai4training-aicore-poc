"""ROI mask editor: browse videos in a folder and draw a per-video mask,
saved as "<video_stem>.mask.png" next to the video for the kinematic
pipeline (src/segmentation/kinematic.py) to auto-detect.

Run: python -m tools.mask_editor.server --dir data --port 8765
"""
